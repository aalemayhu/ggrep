{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "eslint --fix packages/**/*.js",
    "bootstrap": "npm install && ./node_modules/.bin/lerna exec npm install"
  },
  "devDependencies": {
    "lerna": "^3.7.1"
  }
}
